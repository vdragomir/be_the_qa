{
  "name": "be_the_qa",
  "version": "1.0.0",
  "scripts": {
    "cypress": "./node_modules/.bin/cypress",
    "cypress:open": "cypress open",
    "run:tests": "cypress run --headless --env configFile=config"
  },
  "devDependencies": {
    "chai-exclude": "^2.0.2",
    "cypress": "^6.0.0"
  },
  "dependencies": {
    "chai-json-schema": "^1.5.1"
  }
}
